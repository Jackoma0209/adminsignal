import { notFound } from 'next/navigation'

/**
 * Withdrawn. A 200 "editorial review" page is still a low-value screen
 * for an AdSense crawler that may review every URL.
 */
export default function Windows1125H2AutopilotV2Page() {
  notFound()
}
